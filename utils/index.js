const utils = require('./utils');
const action = require('./action');
const config = require('./config');
const locator = require('./locator');
const func = require('./func');

module.exports = {utils, action, config, locator, func};
